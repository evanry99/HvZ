export interface User{
    firstName: string;
    lastName: string;
    isAdmin: boolean;
}

export interface UserResponse{
    id: number;
    firstName: string;
    lastName: string;
    isAdmin: boolean;
}